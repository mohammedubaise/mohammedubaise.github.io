export const API_BASE_URL = window.location.hostname === "localhost" || window.location.hostname === "127.0.0.1"
  ? ""
  : "https://ubaise-portfolio.onrender.com"; // Replace with your actual Render web service URL once deployed
